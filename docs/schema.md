Schema aligne sur src/types/database.ts genere via Supabase CLI.
Tables clefs V1: users_profiles, meals, meal_ingredients, workout_sessions, performed_exercises, exercise_sets, daily_summaries.
RLS: auth.uid() = user_id.
