import { Text } from "react-native";
import { ScreenContainer } from "@/components/ui/ScreenContainer";

export default function SessionDetailScreen() {
  return (
    <ScreenContainer>
      <Text className="text-text">Detail seance (exercices + sets cibles)</Text>
    </ScreenContainer>
  );
}
